"""
Response and contract adapters.

Bridges between the new standardized internal contracts and the legacy
response shapes expected by existing MCP tools and HTTP endpoints.

Modules:
    legacy_response_adapter  – maps new envelope → old dict shapes
    options_contract_adapter – maps native Options API → legacy analytics shape
"""
